// Package version exposes the version embedded in the application binary.
package version

// Version is replaced by GoReleaser when building a published release.
// Local builds intentionally identify themselves as development builds.
var Version = "dev"
